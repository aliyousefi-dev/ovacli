export interface ISelectionBox {
  startX: number; // Horizontal coordinate of the top-left corner
  startY: number; // Vertical coordinate of the top-left corner
  endX: number;
  endY: number;
  isDrawing: boolean;
}
